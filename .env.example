# Server Configuration
PORT=3001
NODE_ENV=development

# Frontend CORS (add your Lovable app URL)
FRONTEND_URL=https://id-preview--6186ccc2-0b8a-4239-9c7c-ab047eb57c12.lovable.app

# Database (leave empty for SQLite in development)
# For production, use PostgreSQL connection string:
# DATABASE_URL=postgresql://user:password@host:5432/database

# Authentication
# Generate a secure random string for production:
# openssl rand -base64 32
API_SECRET_KEY=dev-secret-key-change-in-production

# Polymarket Configuration (for reference only - stored per user in DB)
# CLOB_BASE_URL=https://clob.polymarket.com
