# Relay Server Configuration

# Server Port
PORT=3001

# Environment
NODE_ENV=development

# Authentication secret (must match the one in your frontend Edge Function)
# Generate with: openssl rand -base64 32
API_SECRET_KEY=dev-secret-key-change-in-production

# Frontend CORS (add your Lovable app URL)
FRONTEND_URL=https://your-app.lovable.app

# Polymarket CLOB API
CLOB_API_URL=https://clob.polymarket.com

# Wallet private key for signing (REQUIRED for ClobClient)
# This should be a wallet that has permission to sign for your users
# For production, use a dedicated signing wallet
WALLET_PRIVATE_KEY=your_private_key_here

# Database Configuration
# For production, use PostgreSQL:
DATABASE_URL=postgresql://user:password@host:5432/polymarket_relay

# For development, leave DATABASE_URL empty to use SQLite:
# (SQLite will create relay.db automatically)
